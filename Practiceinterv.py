wait=webdriver.wait(driver,10)
from selenium.webdriver.support import expected_condition as EC
from selenium.webdriver.support.ui import webdriverwait
wait.until(EC.element_to_be_clickable(By.xpath,"someid")

drp=ghghg

print(len(drp.options))


all_options=drp.options

for option in all_options
    print(option.text)

links=driver.find_element(By.Tagname, "a")
    print(len(links))


for link in links
    print(link.text)


 switch_to_alert().accept()
 switch_to_alert().dismiss()


handle=driver.current_window_handles
print(handle)

handles=driver.window_handles

for handle in handles:
    driver.switch_to_window(handle)
    print((driver.title)

print(driver.current_window_handle)


